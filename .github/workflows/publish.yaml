name: Publish Docker Image

on:
  workflow_dispatch:

jobs:
  publish:
    permissions:
      packages: write
      contents: read
    uses: imdevinc/imdevinc/.github/workflows/publish-docker.yaml@v1
